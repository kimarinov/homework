var arr = [-1, 4, -1, 3, -2, 2, 2, -3, 1, 3, -2];
var len = arr.length;
let sum = 0,
    sum2 = 0,
    count = 0;



while(count < len){
	
	//console.log(marker);
	for (var i = 0; i < len; i++) {
  }

	count++;

}
// $each(function(index, el) {
// 	console.log(arr[index]);
// });
$.each(arr, function(index, val) {
	 console.log(index);
});
